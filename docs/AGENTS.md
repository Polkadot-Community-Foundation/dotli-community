# Docs Guide

This directory owns architecture and developer-reference docs for dotli.

## Docs Content Rules

- Order services, providers, and chains alphabetically unless the section is explicitly describing runtime order or auto-detection order.
- Use the same names the code uses (`smoldot-shared-worker`, `BULLETIN_PASEO_GENESIS`).
