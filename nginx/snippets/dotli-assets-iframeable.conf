# /assets/ location for iframeable origins.
# Long cache, immutable hashes. Requires `set $csp_ancestors` upstream.
location /assets/ {
    try_files $uri =404;
    include /etc/nginx/snippets/dotli-headers-iframeable.conf;
    add_header Cache-Control "public, max-age=31536000, immutable";
}
