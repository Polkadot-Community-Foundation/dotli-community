# /assets/ location for the host build.
# Long cache, immutable hashes.
location /assets/ {
    try_files $uri =404;
    include /etc/nginx/snippets/dotli-headers-host.conf;
    add_header Cache-Control "public, max-age=31536000, immutable";
}
