# Host service worker — no-cache, broad scope. Host build headers.
location = /host-sw.js {
    try_files $uri =404;
    include /etc/nginx/snippets/dotli-headers-host.conf;
    add_header Cache-Control "no-cache";
    add_header Service-Worker-Allowed "/";
}
