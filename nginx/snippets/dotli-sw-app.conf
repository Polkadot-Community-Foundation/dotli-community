# App service worker — no-cache, broad scope. Iframeable headers.
location = /app-sw.js {
    try_files $uri =404;
    include /etc/nginx/snippets/dotli-headers-iframeable.conf;
    add_header Cache-Control "no-cache";
    add_header Service-Worker-Allowed "/";
}
