# /t/host → Host build project
location = /t/host {
    limit_except POST { deny all; }
    client_max_body_size 256k;

    proxy_pass https://o4511059872841728.ingest.de.sentry.io/api/4511059878477904/envelope/;
    proxy_ssl_server_name on;
    proxy_set_header Host o4511059872841728.ingest.de.sentry.io;
    proxy_set_header X-Forwarded-For "";
    proxy_set_header Cookie "";
    proxy_buffering off;
    proxy_request_buffering off;
    proxy_connect_timeout 5s;
    proxy_send_timeout 10s;
    proxy_read_timeout 10s;
}

# /t/sandbox → Sandbox build project
location = /t/sandbox {
    limit_except POST { deny all; }
    client_max_body_size 256k;

    proxy_pass https://o4511059872841728.ingest.de.sentry.io/api/4511066636615760/envelope/;
    proxy_ssl_server_name on;
    proxy_set_header Host o4511059872841728.ingest.de.sentry.io;
    proxy_set_header X-Forwarded-For "";
    proxy_set_header Cookie "";
    proxy_buffering off;
    proxy_request_buffering off;
    proxy_connect_timeout 5s;
    proxy_send_timeout 10s;
    proxy_read_timeout 10s;
}

# /t/worker → Worker build project
location = /t/worker {
    limit_except POST { deny all; }
    client_max_body_size 256k;

    proxy_pass https://o4511059872841728.ingest.de.sentry.io/api/4511150390313040/envelope/;
    proxy_ssl_server_name on;
    proxy_set_header Host o4511059872841728.ingest.de.sentry.io;
    proxy_set_header X-Forwarded-For "";
    proxy_set_header Cookie "";
    proxy_buffering off;
    proxy_request_buffering off;
    proxy_connect_timeout 5s;
    proxy_send_timeout 10s;
    proxy_read_timeout 10s;
}
